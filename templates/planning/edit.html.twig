{% extends 'base.html.twig' %}

{% block title %}Edit Planning{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Planning</h1>
   
        {{ include('planning/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('planning/_delete_form.html.twig') }}
       
        <button class="btn btn-dark"> <a href="{{ path('planning_index') }}">back to list</a></button>
    </div>

{% endblock %}
