{% extends 'base.html.twig' %}

{% block title %}Collectivite {% endblock %}

{% block body %}
<br>
    <h2 class="align-items-center">Listes des collectivites </h2>
<br><br>

    <table class="table table-striped table-info">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
              
                
                <th>Nom</th>
                <th>Adresse</th>
                <th>Telephone</th>
                <th>Codepostal</th>
                <th>Ville</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for collectivite in collectivites %}
            <tr>
                <td>{{ collectivite.id }}</td>
                <td>{{ collectivite.email }}</td>
               
                
                <td>{{ collectivite.nom }}</td>
                <td>{{ collectivite.adresse }}</td>
                <td>{{ collectivite.telephone }}</td>
                <td>{{ collectivite.codepostal }}</td>
                <td>{{ collectivite.ville }}</td>
                <td>
                    <a href="{{ path('collectivite_show', {'id': collectivite.id}) }}">Détails</a>
                   
                    <a href="{{ path('collectivite_delete', {'id': collectivite.id}) }}">Effacer</a>

                </td>
            </tr>
           
        {% else %}
            <tr>
                <td colspan="10">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_register') }}">Create new</a>
{% endblock %}
